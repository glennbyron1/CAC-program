# Phase 8 add-on - Ansible inventory for STIG remediation
# Target: LAB-DC01 (Domain Controller + Issuing CA) at 10.10.10.10
# Reached via the LabInternal segment (host is 10.10.10.1 there).
# The DC also has 10.10.20.10; we use .10.10 to avoid the host's adapter conflicts.
#
# Connection details live in group_vars/dc/main.yml (non-secret) and
# group_vars/dc/vault.yml (encrypted password). See README.md.

[dc]
LAB-DC01 ansible_host=10.10.10.10

[dc:vars]
# everything else is set in group_vars/dc/main.yml
